% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/detect.R
\name{cesar_detect}
\alias{cesar_detect}
\title{Detect CNVs in a test sample using a trained CesaR model}
\usage{
cesar_detect(model, depth, sample_id = NULL, min_p = 1e-100)
}
\arguments{
\item{model}{A \code{"cesar_model"} object from [cesar_train()].}

\item{depth}{Numeric vector of length \code{nrow(model$bed)} giving the
test sample's mean depth in each BED segment.}

\item{sample_id}{Optional name for the sample. Defaults to
\code{"(unnamed)"}.}

\item{min_p}{Lower clamp on per-segment p-values before
\eqn{-\log_{10}} transformation, to avoid \code{Inf}.
Default \code{1e-100}.}
}
\value{
An object of class \code{"cesar_result"} -- a list with components
  \code{segments}, \code{genes}, \code{sample}.
}
\description{
Computes anchor-recalibrated depth ratios for every BED segment in
\code{depth}, converts them to per-segment Z-scores against the
training-time mean/SD, and aggregates by gene.
}
\details{
Unlike the older Cesar package, CesaR's detect step accepts only a
precomputed depth vector. Build it with [bed_depth_in_pileup()] applied
to the test sample's pileup.
}
\seealso{
[cesar_train()]
}
